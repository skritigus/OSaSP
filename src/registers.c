#include "registers.h"
#include <stdio.h>

/*void setRegister(CPU *cpu, int reg, int value) 
{
    if (reg >= 0 && reg < REGISTER_COUNT) 
    {
        cpu->registers[reg] = value;
    } 
    else 
    {
        printError("Invalid register index");
    }
}

int getRegister(CPU *cpu, int reg) 
{
    if (reg >= 0 && reg < REGISTER_COUNT) 
    {
        return cpu->registers[reg];
    } 
    else 
    {
        printError("Invalid register index");
        return 0;
    }
}*/
